// = กำหนดค่า ให้ตัวแปร
// == เปรียบเทียบว่า ค่า ของสองสิ่งเท่ากันหรือไม่
// === ใช้เพื่อเปรียบเทียบว่า ค่าและประเภทข้อมูล เท่ากันหรือไม่
// != เปรียบเทียบว่า ค่า ของสองสิ่งไม่เท่ากัน
// !== เปรียบเทียบว่า ค่าและประเภทข้อมูล ของสองสิ่งไม่เท่ากัน

const PI = 3.14;

if(PI === "3.14"){
    console.log("Thats Pi");
}
else{
    console.log("Thats not pi");
}
